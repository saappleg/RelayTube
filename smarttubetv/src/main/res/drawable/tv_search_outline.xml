<?xml version="1.0" encoding="utf-8"?>
<!-- Unfocused outline used by the v21 search selector. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/relay_surface_variant" />
    <corners android:radius="30dp" />
    <stroke
        android:width="1dp"
        android:color="@color/relay_outline_variant" />
</shape>
